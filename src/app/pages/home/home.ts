import { Component } from '@angular/core';
import { Layout } from "../../components/layout/layout";

@Component({
  selector: 'app-home',
  imports: [Layout],
  templateUrl: './home.html',
  styleUrl: './home.scss',
  standalone: true
})
export class Home {

}
